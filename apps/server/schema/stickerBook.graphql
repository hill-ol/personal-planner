type Sticker {
    id: ID!
    name: String!
    imageUrl: String!
    unlockedAt: DateTime!
    unlockTrigger: String
    positionX: Float!
    positionY: Float!
    rotation: Float
}

type StickerBook {
    id: ID!
    stickers: [Sticker!]!
}