type Person {
    id: ID!
    name: String!
}

type SocialEvent implements Item {
    id: ID!
    name: String!
    status: ItemStatus!
    startDate: DateTime!
    time: Time
    endDate: DateTime!
    recurrence: Recurrence
    invitees: [Person!]
    location: String
}