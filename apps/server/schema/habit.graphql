type HabitCompletion {
    id: ID!
    completedAt: DateTime!
}

type Habit {
    id: ID!
    name: String!
    recurrence: Recurrence!
    currentStreak: Int!
    longestStreak: Int!
    completions: [HabitCompletion!]!
}